-- Create a generic table as example
CREATE TABLE IF NOT EXISTS table_name (
    id INT,
    name VARCHAR(256) NOT NULL
);

-- Create the force_name table with id and name columns
CREATE TABLE IF NOT EXISTS force_name (
    id INT,
    name VARCHAR(256) NOT NULL
);